services:
  app:
    build: .
    container_name: "${COMPOSE_PROJECT_NAME:-runboard}"
    environment:
      DATABASE_URL: "${DATABASE_URL:-file:/app/data/prod.db}"
    volumes:
      - data:/app/data
    ports:
      - "${APP_PORT:-3000}:3000"
    restart: unless-stopped

volumes:
  data:
